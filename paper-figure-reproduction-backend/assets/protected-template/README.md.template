# __PLOT_FAMILY__ — __PAPER_IDENTITY__ Fig. __FIGURE_PANEL__

Status: `__STATUS__`

This package reproduces __PAPER_IDENTITY__, Fig. __FIGURE_PANEL__, using the author-supported __LANGUAGE__ workflow. Scientific inputs and source status are defined in `docs/SCIENTIFIC_CONTRACT.md` and `source_manifest.json`.

## Quick start

From the package root, run `__MAIN_COMMAND__`. Replay the backend with `__CLI_COMMAND__`.

## Inputs and outputs

Routine plotting reads compact Figure-ready files from `example_input/`. Acquisition or expensive preparation, when needed, lives under `prepare/`. Current evidence and figures are written under `example_output/`; the success marker is written last.

## Reproduction boundary

The scientific figure is generated by code. Reference images are used only for comparison. Known uncertainty is recorded in `docs/REPRODUCTION_NOTES.md`.


